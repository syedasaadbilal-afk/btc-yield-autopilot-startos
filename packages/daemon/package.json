{
  "name": "@autopilot/daemon",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "start": "node --experimental-sqlite --loader ts-node/esm src/index.ts"
  },
  "dependencies": {
    "@autopilot/shared": "*",
    "@autopilot/strategy": "*",
    "@autopilot/execution": "*",
    "@autopilot/bitfinex-client": "*",
    "fastify": "^5.1.0",
    "@fastify/static": "^8.0.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "@types/node": "^22.9.0"
  }
}
